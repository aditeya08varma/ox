---
title: "ox carts update"
description: "CLI reference for ox carts update"
sidebar_position: 25
---

## ox carts update

Update a cart

```
ox carts update ID [flags]
```

### Options

```
  -a, --assignee string      New assignee
  -d, --description string   New description
  -h, --help                 help for update
  -p, --priority int         New priority (default -1)
      --status string        New status
      --title string         New title
  -t, --type string          New type
```

### Options inherited from parent commands

```
  -c, --config string    config file path (default: .sageox/config.yaml)
      --json             output in JSON format (default: false)
      --no-interactive   disable spinners and TUI elements (auto-enabled when CI=true)
      --profile          generate CPU profile and execution trace for performance analysis (default: false)
  -q, --quiet            suppress non-error output (default: false)
  -v, --verbose          enable verbose output (default: false)
```

### SEE ALSO

* [ox carts](/carts)	 - Manage carts — trackable work items backed by DoltDB

